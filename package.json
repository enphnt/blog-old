{
  "name": "blog",
  "description": "Nathan Phennel's blog which uses gatsby+gh-pages to simplify the boring bits.",
  "version": "2.0.0",
  "author": "Nathan Phennel <nathanphennel@gmail.com>",
  "dependencies": {
    "gatsby": "^2.32.13",
    "gatsby-link": "^2.6.46",
    "gatsby-plugin-catch-links": "^2.0.24",
    "gatsby-plugin-react-helmet": "3.0.11",
    "gatsby-source-filesystem": "^2.5.39",
    "gatsby-transformer-remark": "^2.11.1",
    "react": "^16.14.0",
    "react-dom": "^16.14.0",
    "react-helmet": "^5.2.0",
    "react-media": "^1.8.0"
  },
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "develop": "gatsby build && gatsby develop",
    "format": "prettier --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0",
    "prettier": "^1.12.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/enphnt/blog"
  }
}
